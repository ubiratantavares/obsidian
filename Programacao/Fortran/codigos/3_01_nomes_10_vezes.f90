!
! Mostra o nome de uma pessoa dez vezes - versão sem repetição
! OBS: Quando aumenta o número de vezes, aumenta o tamanho do programa
!
PROGRAM nome_10_vezes

    ! Um WRITE para cada vez que o nome é impresso
    WRITE (*, *) "Meu nome completo"  ! Mostra o nome uma só vez
    WRITE (*, *) "Meu nome completo"  ! Mostra o nome uma só vez
    WRITE (*, *) "Meu nome completo"  ! Mostra o nome uma só vez
    WRITE (*, *) "Meu nome completo"  ! Mostra o nome uma só vez
    WRITE (*, *) "Meu nome completo"  ! Mostra o nome uma só vez
    WRITE (*, *) "Meu nome completo"  ! Mostra o nome uma só vez
    WRITE (*, *) "Meu nome completo"  ! Mostra o nome uma só vez
    WRITE (*, *) "Meu nome completo"  ! Mostra o nome uma só vez
    WRITE (*, *) "Meu nome completo"  ! Mostra o nome uma só vez
    WRITE (*, *) "Meu nome completo"  ! Mostra o nome uma só vez
    STOP  ! Pára após a impressão do nome 10 vezes
END PROGRAM nome_10_vezes
