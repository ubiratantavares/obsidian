     program escrever
     implicit none
     integer a, b, c, sum
     print*, 'entre com os valores de a, b e c'
     read(*,*) a, b, c
     sum = a + b + c
     sum = a + b + c
     write (*,*) a, b, c, sum
     stop
     end